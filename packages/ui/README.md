# ui

`ui` 是共享 UI 基础包骨架，未来用于承载通用原始组件、设计令牌、样式基础和可复用界面约定。

当前阶段只保留最小可消费结构：

- 已有 `package.json`
- 已有 `src/` 目录
- 已有 `src/primitives/` 与 `src/tokens/` 占位导出

本阶段不迁移任何真实 React 组件，不接入样式构建链，不补充业务页面能力，也不把现有实现迁入此包。

后续真正接入时，再由对应前端分支补齐组件实现、样式变量和测试约定。
