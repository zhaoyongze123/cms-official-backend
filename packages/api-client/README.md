# api-client

`api-client` 是面向 CMS 业务的 API 客户端共享包骨架，未来用于承载对 Django 公开 API 的统一封装、错误类型、请求上下文和类型定义。

当前阶段只保留最小可消费结构：

- 已有 `package.json`
- 已有 `src/` 目录和占位导出
- 已有最小错误、HTTP 和 CMS API 客户端占位类型

本阶段不迁移任何真实请求实现，不接入构建工具链，不新增业务逻辑，也不直接依赖运行时网络请求。

后续真正接入时，再由对应业务分支补充请求实现、响应映射、错误归一化和测试覆盖。
