# shared-types

这是共享类型包的最小骨架，只保留后续可引用的类型入口，不承载业务逻辑。

## 当前导出范围

- `src/article.ts`：文章相关占位类型。
- `src/seo.ts`：SEO 相关占位类型。
- `src/analytics.ts`：分析相关占位类型。
- `src/ai-review.ts`：AI 评审相关占位类型。

这些文件目前只提供最小占位类型，后续如需扩展，应由对应契约或上层模块驱动，不在这里提前发明复杂结构。
