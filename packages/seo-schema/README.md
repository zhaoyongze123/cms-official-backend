# seo-schema

这是 SEO 结构化数据包的最小骨架，只保留后续可消费的入口，不包含真实生成逻辑。

## 与 `contracts/` 的映射

- `src/article.ts`：文章结构化数据的占位签名，后续对接文章相关契约。
- `src/faq.ts`：FAQ 结构化数据的占位签名，后续对接 FAQ 相关契约。
- `src/breadcrumb.ts`：面包屑结构化数据的占位签名，后续对接导航相关契约。

当前阶段只保留签名和导出入口，不在这里生成 JSON-LD，也不内置业务字段。
