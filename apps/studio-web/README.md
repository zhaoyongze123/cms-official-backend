# studio-web 主工程

`apps/studio-web/` 是当前 Next.js 运营工作台的主工程目录。

当前状态是：

- `apps/studio-web/` 持有真实工程代码、配置、页面、组件、`lib/` 和测试。
- 构建、路由和 Mock API 行为保持不变。

基础验证：

```bash
cd apps/studio-web
npm run lint
npm run test
npm run build
```
