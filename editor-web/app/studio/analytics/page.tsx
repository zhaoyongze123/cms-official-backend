export default function AnalyticsPlaceholderPage() {
  return (
    <div className="page-stack">
      <section className="hero">
        <span className="eyebrow">Analytics Placeholder</span>
        <h1>A11 监控面板入口已预留。</h1>
        <p>
          当前页面只固定 Studio 路由位置和视觉骨架，不提前接入 AnalyticsSnapshot、GA4 或 GSC 数据。
          后续分支可以直接在此页填入 mock 指标卡片和趋势图。
        </p>
      </section>
    </div>
  );
}
